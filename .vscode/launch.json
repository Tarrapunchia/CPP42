{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "cppvsdbg", // Cambia in "cppdbg" se usi GDB
            "request": "launch",
            "program": "${workspaceFolder}a.exe", // Modifica con il tuo programma
            "args": [
                "./files/test", 
                "lorem",
                "CANE"
            ],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb", // Cambia in "gdb" se necessario
            "preLaunchTask": "build"
        }
    ]
}
